// components/nav.component.tsx
import React from 'react';
import styles from './nav.style.module.scss';

const Nav: React.FC = () => {
  return (
    <nav className={styles.nav}>
      {/* Placeholder for navigation */}
    </nav>
  );
};

export default Nav;
