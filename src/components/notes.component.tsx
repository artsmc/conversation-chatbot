// components/notes.component.tsx
import React from 'react';
import styles from './notes.style.module.scss';

const Notes: React.FC = () => {
  return (
    <div className={styles.notes}>
      {/* Placeholder for notes */}
    </div>
  );
};

export default Notes;
